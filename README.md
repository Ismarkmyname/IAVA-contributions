# IAVA2
How to use?
run both django and fastapi on different terminal and different port example:
django: py manage.py runserver 8001 python manage.py runserver 8001
fastapi: uvicorn main:app --reload